apiVersion: v1
kind: Service
metadata:
  name: frictionlens-frontend
  namespace: frictionlens
  labels:
    app: frictionlens-frontend
spec:
  selector:
    app: frictionlens-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
