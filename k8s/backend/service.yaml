apiVersion: v1
kind: Service
metadata:
  name: frictionlens-backend
  namespace: frictionlens
  labels:
    app: frictionlens-backend
spec:
  selector:
    app: frictionlens-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
